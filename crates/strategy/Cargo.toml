[package]
name = "strategy"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
exchange = { path = "../exchange" }
fsm = { path = "../fsm" }
ledger = { path = "../ledger" }
rust_decimal = { workspace = true }
rust_decimal_macros = { workspace = true }
