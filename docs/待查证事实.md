# 待查证事实清单（Polymarket 交易所真实行为）

> **用途**：集中记录所有需要核实的 Polymarket 真实行为。
> 凡涉及交易所规则的代码实现，都应先来这里确认。
>
> 状态图例：✅ 已核实 ｜ ⚠️ 已用推测值（待核实）｜ ❓ 完全未知（待核实）

---

## 一、已核实

| # | 事实 | 结论 | 核实方式 | 影响的代码 |
| --- | --- | --- | --- | --- |
| 1 | **Taker 手续费率** | 约 3% 多，按 4% 计提（略偏保守） | 用户实测 | `FeeModel` 默认 taker 4% |
| 2 | **手续费扣费方式** | 扣股数（下单 100 股价 0.5 费率 4% → 实付现金 50、净入仓 96 股） | 用户实测 | `FeeModel::net_qty`、`Fill`、`Ledger` |
| 3 | **最小下单量** | 最少 5 份代币，且最少 1 美元（须同时满足） | 用户提供 | `OrderConstraints` |
| 4 | **价格/数量精度** | Maker 最高 2 位小数；Taker 4~6 位。全局统一用 2 位 | 用户查证 | `OrderConstraints` price_scale/size_scale=2 |

---

## 二、已用推测值，待核实 ⚠️

| # | 事实 | 当前推测/默认 | 风险 | 影响的代码 |
| --- | --- | --- | --- | --- |
| 5 | **Maker 手续费率** | 默认 0（零费） | 若 Maker 也收费，PnL 会偏乐观 | `FeeModel` maker_fee_rate=0 |
| 6 | **gamma API `makerBaseFee`/`takerBaseFee` 字段含义** | 合约源码(`ctf-exchange/Fees.sol`)明确 `MAX_FEE_RATE_BIPS=1000` 是合约允许的最大费率上限（10%），不是实际收费。`py-clob-client` 默认 `fee_rate_bps=0`。实际费率仍以 #1 为准 | 不影响代码，影响策略分析 | — |

---

## 三、完全未知，待核实 ❓

> 接真实交易所前必须查清。

| # | 待查问题 | 为什么重要 |
| --- | --- | --- |
| 7 | **撤单失败时 Polymarket 返回什么？** | 决定是否需要"撤单失败"事件变体 |
| 8 | **成交回报与订单状态变更是否走同一个 WebSocket 频道？** | 决定事件接收结构 |
| 9 | **是否存在部分成交？** | ✅ 已确认：Polymarket Maker 单部分成交后剩余继续在订单簿排队。代码已按此实现（book.apply_fill 减 qty，全成交才移除）。 |
| 10 | **订单状态枚举值与回调消息结构？** | 决定 `ExchangeEvent` 设计 |
| 11 | **价格 tick size 是否随市场不同？** | 决定挂单价是否需额外按 tick 对齐 |
| 12 | **结算/交割机制**：何时结算、结算价来源、上轮持仓如何兑现、新轮 token id 如何获取 | 决定跨轮衔接逻辑 |
